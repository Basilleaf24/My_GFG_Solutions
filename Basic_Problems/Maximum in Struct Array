int findMax(Height arr[], int n)
{
    // Code here
    int i, max, temp;
    max=arr[0].feet+arr[0].inches;
    for(i=0; i<n; i++)
    {
        
        temp=(arr[i].feet)*12 + arr[i].inches;
        
        if(temp>max)
            max=temp;
    }
    return max;
}
