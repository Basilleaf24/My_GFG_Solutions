int isLengthEvenOrOdd(struct Node* head)
{
     //Code here
    int count=0;
    if(head==NULL) return 0;
    Node *temp=head;
    while(temp!=NULL)
    {
        temp=temp->next;
        count++;
    }
    if(count%2==0)return 0;
    return 1;

}
