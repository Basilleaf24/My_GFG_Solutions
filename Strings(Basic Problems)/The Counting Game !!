#include <iostream>
using namespace std;

int main() {
	//code
	int t;
	cin>>t;
	while(t--)
	{
	    string s;
	    cin>>s;
	    int count=1, i;
	    for(i=0; i<s.length(); i++)
	        if(s.at(i)>=65 && s.at(i)<=90)
	            count++;
	    cout<<count<<endl;
	}
	return 0;
}
