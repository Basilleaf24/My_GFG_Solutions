#include <iostream>
using namespace std;

int main() {
	//code
	int t;
	cin>>t;
	while(t--)
	{
	    string s;
	    cin>>s;
	    int i, count=0;
	    for(i=0; i<s.length()-1; i++)
	    {
	        if(s.at(i)=='2' && s.at(i+1)=='1')
	            count++;
	    }
	    cout<<count<<endl;
	}
	return 0;
}
